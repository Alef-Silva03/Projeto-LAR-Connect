//Este arquivo cria a classe Perfil, que é um tipo de variável que só pode assumir 4 valores: PROPRIETARIO, INQUILINO, SINDICO e FUNCIONARIO.

package com.projeto.larconnect.model;

public enum Perfil {								
	PROPRIETARIO, INQUILINO, SINDICO, FUNCIONARIO;
}
